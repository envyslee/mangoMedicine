<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/toolbarColor</item>
        <item name="colorPrimaryDark">@color/smssdk_black</item><!--状态栏底色，这里设置了貌似没有效果-->
        <item name="colorAccent">@color/beautyGreen</item>
        <item name="android:windowBackground">@color/pageBackground</item>
    </style>

    <style name="StartAppTheme" parent="AppTheme">
        <item name="android:background">@drawable/layout_list_start_pic</item>
    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
